# Claude Code local settings — machine-specific, never shared
.claude/settings.local.json

# Implementation spec — kept local, never pushed
PLAN.md

# OS metadata
.DS_Store
Thumbs.db
desktop.ini

# Editor state
.vscode/
.idea/
*.swp

# Dependencies
/node_modules

# Next.js build output
/.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# Test output
/coverage

# Screenshots from the driver scripts. SHOT_DIR defaults to the repo root, so
# every run of scripts/drive-*.mjs drops these here.
/room-*.png
/heatmap.png
/mobile-*.png

# Environment — never commit real credentials, see .env.example
.env*
!.env.example

# Deployment
.vercel

# Logs
npm-debug.log*
